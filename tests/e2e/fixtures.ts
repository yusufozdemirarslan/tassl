// Playwright fixtures (docs/tech/14-testing-strategy.md §2).
// signInAs(), runToState(), and advanceClock() are added in Phase 3 and later.
export { expect, test } from '@playwright/test'
export { axe } from './a11y/axe'
