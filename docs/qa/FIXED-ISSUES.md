# Fixed issues

Every defect found in the QA run (docs/prompts/02-qa-and-guides.md rule 7). Status is `fixed` for every row.

| ID | Severity | Where a judge would have seen it | Root cause | Fix commit | Guarding test | Status |
|---|---|---|---|---|---|---|
| QA-001 | P1 | `https://tassl.vercel.app/robots.txt` answered with the 404 page; a crawler had no instruction to stay away | No `robots` route existed; only the `noindex` meta on each page | c06828f | `tests/e2e/smoke/health.spec.ts` "/robots.txt keeps the deployment out of search engines" | fixed |
| QA-002 | P2 | `/api/health` in production reported `"version":""`, so nobody could tell which commit was live | The route read `VERCEL_GIT_COMMIT_SHA ?? 'dev'`, and a prebuilt deploy whose `.git` production.yml detaches gets that variable set to the empty string rather than left unset, so the fallback was never reached | c06828f | `tests/e2e/smoke/health.spec.ts` "/api/health answers 200 with the build version" (`SMOKE_EXPECT_VERSION` in the production workflow) | fixed |
| QA-003 | P1 | On the assignment page the instructor had no way to open a run's replay; the table printed "The replay of a scored run … arrives with the review screens" — a confirmed run, which leaves the review queue, could only be reached by typing its id | The Phase-6 placeholder was never replaced when the review screens arrived (Phase 11) | c06828f | `tests/e2e/guides/instructor-guide.spec.ts` Task 9 step 4 ("Open the replay for Student Two") | fixed |
| QA-004 | P1 | A student rehearsing "Decision Run 1 (sound)" or "Auto-lock test run" could not run it again on the same seat: the run could not be deleted, only voided | The seed flagged only the first assignment `isWalkthrough` | c06828f | `tests/integration/db/seed.test.ts` (three walkthrough assignments) and `scripts/demo-reset.ts` | fixed |
| QA-005 | P0 | On the Turn screen the claim the Turn is about (C3) was missing when the student had already met it: the window listed only C2, so the student could not take the stance the Turn asked for | `upsertRunClaim` left an existing row untouched even when the surfacing was a Turn-window one, so `in_turn_window` stayed false (D-705) | b2730b8 | `tests/integration/runs/turn.test.ts` "marks the existing row in_turn_window and keeps one row per claim"; demo path row 14 | fixed |
| QA-006 | P1 | Pressing Close on the correction dialog right after "What the correction moved" appeared was ignored on some engines, leaving the instructor in the dialog | State updates after an `await` inside a `useTransition` landed one commit before `pending` fell, and the dialog's guard refused the close in that gap (D-706) | b2730b8 | `tests/e2e/guides/demo-path.spec.ts` row 22 on chromium, firefox and webkit | fixed |
| QA-007 | P1 | A classmate who guessed a run id learned it existed: the replay, bands, exports, corrections and void endpoints answered 403 to a section student who did not own the run | `requireRunReviewer`, `requireRunInstructor` and `requireCourseExportReader` answered FORBIDDEN to every section member with the wrong role (D-703) | 2a353b1 | `tests/integration/api/review.test.ts` "refuses the run's own student, a classmate, and a signed-out caller" (classmate → 404); `tests/integration/authz-matrix.test.ts` IDOR | fixed |
| QA-008 | P1 | Failed sign-ins for one account were limited per client address only; a credential-stuffing run spread over addresses was never refused (D-021 named a per-account limit that did not exist) | No per-account counter; Better Auth's limiter is per address (D-704) | 2a353b1 | `tests/integration/auth/account-lockout.test.ts` | fixed |
| QA-009 | P1 | A request body of any size reached JSON parsing and Zod; a hostile 100 MB body was work for the function before it was refused | No body cap on route handlers (D-702) | 2a353b1 | `tests/integration/api/envelope.test.ts` "answers 413 PAYLOAD_TOO_LARGE to a body over the cap" | fixed |
| QA-010 | P2 | The perf spec skipped itself on firefox and webkit, so the suite reported "2 skipped" on every run (rule 8 forbids `test.skip`) | LCP and layout-shift observers exist only in Chromium; the exclusion was written inside the test rather than in the project configuration | c06828f | `playwright.config.ts` `testIgnore` on the firefox and webkit projects; the suite reports 0 skipped | fixed |
| QA-011 | P1 | Two endpoints had no test at all (`GET /notifications/unread-count`, `POST /runs/{id}/readiness/skip`), and nothing checked that every endpoint is tested | The documented API-coverage gate (`14-testing-strategy.md` §3) was never written | 8b2575a | `tests/integration/api/coverage.test.ts`; the two new cases in `notifications.test.ts` and `runs/readiness.test.ts` | fixed |
| QA-012 | P1 | Self-service sign-up in production could never complete: the verification email went to the console transport (no sending domain), so a judge who signed up was stuck on "check your email" | No demo mode existed (D-692) | c06828f | `tests/unit/auth/auth-options.test.ts`, `tests/unit/components/auth/sign-up-form.test.tsx`; production `DEMO_MODE=true` | fixed |
| QA-013 | P1 | Switching the assistant off in production took a redeploy (41 minutes through the pipeline); a presenter facing a slow model had no faster control | The provider was chosen from the environment alone (D-691) | c06828f | `tests/unit/llm/ai-mode.test.ts`, `tests/integration/admin/ai-mode.test.ts`, `tests/e2e/smoke/screens.spec.ts` "the admin sees the assistant mode" | fixed |
| QA-014 | P2 | The sign-in `next` parameter had no test proving an absolute URL cannot redirect a signed-in person off the site | The guard existed (`resolveNext`) without a test | b2730b8 | `tests/e2e/security/headers.spec.ts` "open redirect on sign-in" (four hostile values) | fixed |
| QA-015 | P1 | A signed-in person from another institution (or the platform admin) who tried a section id learned that it existed: `GET /review/sections/{id}/runs` answered 403 across tenants instead of 404 | `requireSectionRole` answered FORBIDDEN to every non-member, never asking which institution the section belonged to (D-710) | bb17c4a | `tests/integration/auth/permissions.test.ts` "denies the wrong role with FORBIDDEN, and an outsider with NOT_FOUND"; `tests/integration/authz-matrix.test.ts` IDOR "section runs for review" | fixed |
| QA-016 | P1 | On Firefox, two stance chips pressed a hundred milliseconds apart flashed "Something went wrong" between two clicks that had both succeeded (the student guide's Task 7 failed on it every time) | Two `router.refresh()` calls overlapped; Firefox reports the cancelled response stream as `TypeError: Error in input stream`, which React logged and the route's error boundary rendered (D-709) | 851ef96 | `tests/unit/hooks/use-refresh.test.tsx`; the guide chain on firefox (`tests/e2e/guides/learner-guide.spec.ts` Task 7 under the console-error guard) | fixed |
| QA-017 | P2 | A failed sign-out bounced the person back to Home with no message | `use-sign-out.ts` ignored the error the auth client returned | 851ef96 | `tests/unit/lib/use-sign-out.test.tsx` | fixed |
| QA-018 | P2 | The rail did not mark **Admin** as the current section on the Flags and Audit log pages | The rail matched one exact href instead of the section prefix | 851ef96 | `tests/unit/components/layout/rail.test.tsx` | fixed |
| QA-019 | P2 | Nothing inside the signed-in app linked to the Privacy or Terms pages | The links existed only in the public footer | 851ef96 | `tests/unit/components/layout/account-menu-popup.test.tsx`; guide Tasks 1 and 12 (instructor), 16 (student) | fixed |
| QA-020 | P2 | Notification emails said "Email copies can be turned off in your account settings", and no such setting exists | Copy written before the setting was decided against (it is a deployment variable) | 851ef96 | `tests/unit/email/templates.test.tsx` "the notification footer" | fixed |
| QA-021 | P2 | The runs list said **Continue** for a run that had not begun, whose screen is "Before you begin" | The action label did not distinguish `assigned` from later states | 851ef96 | `tests/unit/components/run/run-list.test.tsx` | fixed |
| QA-022 | P2 | A held run read **Under review** on the assignment page but **Defense complete** on the review queue and the replay header | The queue and the replay header did not pass the held flag to the state chip | 851ef96 | `tests/unit/components/review/review-queue.test.tsx`, `tests/unit/app/replay-page.test.tsx` | fixed |
| QA-023 | P2 | The generation screen's retry copy said generation "starts again at step 1; it does not resume", the opposite of what it does since D-683 | Copy not updated with the resume | 851ef96 | `tests/unit/components/packages/generation-progress.test.tsx` | fixed |
| QA-024 | P1 | Package rule failures, the import dialog and the confirmation record showed element row ids (UUIDs) where the author needed keys such as C3 or D1 | The element key was not carried through the validation result and the record view | 851ef96 | `tests/unit/scenarios/validate.test.ts` ("elementKeys"), `tests/unit/components/packages/confirmation-record.test.tsx`, `tests/integration/scenarios/lifecycle.test.ts` | fixed |
| QA-025 | P2 | When applying a grade mapping failed, the editor showed the explanatory note instead of the failure | The rejection branch set the wrong message | 851ef96 | `tests/unit/components/courses/mapping-editor.test.tsx` (two cases) | fixed |
| QA-026 | P2 | An instructor's Home showed a "Your runs" panel reading "Nothing to do yet" | The panel rendered for every role | 851ef96 | `tests/unit/app/home-page.test.tsx` | fixed |
| QA-027 | P2 | Signed-in devices on the Security screen showed a raw User-Agent string as the device name | No parser existed | 851ef96 | `tests/unit/lib/user-agent.test.ts` (Chrome, Firefox, Safari, Playwright), `tests/unit/components/account/session-list.test.tsx` | fixed |
| QA-028 | P1 | The Turn screen had no outside-tool declaration control although the Turn window is a working period (FR-061) | The control was mounted on the work page only | 851ef96 | `tests/unit/app/turn-page.test.tsx` | fixed |
| QA-029 | P2 | Filing the two debrief answers gave no confirmation beyond the changed panel ("Both answers are filed." existed in the catalogue and was never shown) | Dead catalogue key | 851ef96 | `tests/unit/components/debrief/debrief-questions.test.tsx` | fixed |
| QA-030 | P2 | "Scenario" and "Package" named one thing on different screens, and the export table said "Student seat" beside a table that said "Student" | Terminology drift across five message namespaces | 851ef96 | `tests/unit/copy/terminology.test.ts`; instructor guide Task 10 step 6 | fixed |
| QA-031 | P1 | A refresh asked for while another was in flight was dropped when the component that started the flight was unmounted by its own refresh: confirming a second package element a hundred milliseconds after the first left "51 of 93 confirmed" on screen (three of three repeats on chromium) | `useRefresh()` (D-709) ended a flight only from the starter's own transition; an unmounted starter never reported, and the remembered refresh waited for a call that never came | a6bce3d | `tests/unit/hooks/use-refresh.test.tsx` (unmounted starter; stale-bound timer); `tests/e2e/author/generate-and-confirm.spec.ts` | fixed |
| QA-032 | P1 | Better Auth admitted ten sign-ins a minute per client address, so the eleventh student of a section signing in from one campus address in the same minute was refused with 429 (NFR-014: sixty students at once) | The sign-in ceiling was sized against guessing, which the per-account lockout (D-704) already stops, not against a class arriving together | a6bce3d | `tests/integration/auth/flows.test.ts` ("admits a section from one address — 120 sign-ins in a minute — and refuses the 121st"); `tests/load/core-flow.js` arrives over one minute | fixed |
| QA-033 | P2 | The open-redirect check's backslash case tested `/evil.example`, a same-site path the sign-in form rightly honours, and so failed on every engine | An editor had stripped the backslash from the test's string literal | a6bce3d | `tests/e2e/security/headers.spec.ts` builds the case from the character code | fixed |
| QA-034 | P2 | The browser smoke check "the instructor signs in and opens Courses and Review" failed on a database the suites had added courses to, because it assumed the seeded course sits on the first page | The check read the page it happened to be on rather than the list's paging | a6bce3d | `tests/e2e/smoke/screens.spec.ts` walks "Show more courses" until the seeded course is on screen | fixed |
| QA-035 | P3 | The flag table and the model-usage table on /admin/flags forced a 672 px minimum width, so on a phone their last column sat behind a horizontal scroll inside the panel | A fixed minimum width on tables whose columns wrap well | a6bce3d | Impeccable audit at 390×844 (`docs/qa/PROGRESS-QA.md` C11 evidence); `tests/e2e/a11y/admin.spec.ts` | fixed |
| QA-036 | P2 | The accessibility scan of the student run screens failed one run in three with a contrast ratio of 4.19:1 on a button that clears 4.5:1 in both its states: the scan read the button mid-fade, inside a dialog that was closing | `axe()` ran the instant after an action, while a 150 ms transition or a dialog's fade was still blending two colours | 772f8fc | `tests/e2e/a11y/axe.ts` awaits every finite animation (capped at two seconds) before scanning | fixed |
| QA-037 | P2 | The keyboard-only run failed in the Turn: Tab never reached "the stance group on C3" because the walk aimed at the group's first chip, and after D-705 a claim raised into the window keeps its working-period stance, so the tabbable chip was Escalate | A roving-tabindex group has one tabbable chip, and the test assumed it was the first | 772f8fc | `tests/e2e/a11y/keyboard-only-run.spec.ts` walks to the chip with `tabindex="0"` and presses Space only when nothing is checked | fixed |
| QA-038 | P1 | Across the three guides, 110 steps named controls by their accessible names ("Open the replay for Student Two", "Course views", "Time until the Turn", "Configure Decision Run 1 (walkthrough)") that a reader never sees on screen, and 90 screenshots showed the top of a page while the step described something below the fold | The guides were written from the specs' locators, and the capture never scrolled | 772f8fc | `shot(task, step, show)` in `tests/e2e/guides/fixtures.ts` scrolls the step's subject into view (D-714); every step names visible text; `scripts/check-guide-coverage.ts` keeps guides and specs in step; the chromium chain recaptures every image | fixed |
| QA-039 | P1 | The demo runbook stated five things that were false: `pnpm demo:warm` against production without the production seed password, two of the three assignments "not walkthrough assignments" (all three are), the smoke script printing `SMOKE OK` (it prints `smoke passed for <base>`), a shared venue address as the cause of a sign-in refusal (the cause is the per-account lockout), and `pnpm demo:reset` leaving every student seat unused (it builds two runs on student2) | Prose written before the facts it described were settled, and never re-read against the code | 772f8fc | `docs/guides/demo-runbook.md` rows and sections corrected; the runbook table and `tests/e2e/guides/demo-path.spec.ts` step titles are checked together by `scripts/check-guide-coverage.ts` | fixed |
| QA-040 | P2 | "holds at ten calls a minute on the llm bucket" failed in CI with the eleventh call admitted: the sliding window weights the previous minute by the share of the current one that has passed, so eleven slow streamed calls straddling a minute boundary count as fewer than ten | A timing-dependent test of an approximate window | 772f8fc | `tests/integration/assistant/delegate.test.ts` holds `Date` one second past a minute boundary for the loop | fixed |
| QA-041 | P2 | On an eight-core laptop Playwright ran four browsers against one Postgres and one Next server, and a Firefox test that reloads three times ran past its sixty seconds with every assertion true | Playwright's default worker count is half the cores; CI has two | 772f8fc | `playwright.config.ts` pins two workers (D-715) | fixed |
| QA-042 | P1 | On Safari, pressing **Export package JSON** replaced the package screen with a wall of raw JSON and saved nothing; the only way back was the Back button. The same for the replay's per-version exports | The four export anchors leaned on `content-disposition: attachment` alone, and WebKit committed the click as a navigation instead. The screen's own comment asserted the opposite ("so the browser saves the file instead of navigating away") | (this commit) | `tests/e2e/guides/instructor-guide.spec.ts` Task 5 step 10 and `learner-guide.spec.ts` Task 16, on chromium, firefox and **webkit** — the webkit chain had never got past this step (D-719) | fixed |
| QA-043 | P1 | Pressing **Finish it** at the end of the defense landed one student in three on the debrief instead of the **Run status** screen the learner guide and the runbook both promise, so they never read FR-140's "The bands in it are drafts until your instructor confirms them" before seeing their bands | The completion refreshed the page and let the defense guard re-read the run's state to decide where to send them; the same press enqueues scoring, which the request drains in its own `after()` (D-410), so a fast scorer beat the render and the guard read `scored` | (this commit) | `tests/unit/components/run/defense-question.test.tsx` ("files the unanswered questions empty and then completes" now asserts `router.replace('/runs/<id>')`); `tests/e2e/walkthrough/10-defense.spec.ts`, `16-sound-variant.spec.ts`, `a11y/keyboard-only-run.spec.ts`, `guides/learner-guide.spec.ts` and `guides/demo-path.spec.ts` all wait on that URL (D-720) | fixed |
| QA-044 | P1 | Every student who closed a tab on a run screen would have opened a new Sentry issue and paged the builder: 13 §7's `NFR-007 error burst` rule fires on a new untagged issue, or ten untagged events in five minutes, and one headless browser walking the guides produced twelve in six minutes | Next cancels a pending render with a plain `Error('The destination stream closed early.')` when the response closes, once per unfinished Suspense boundary; its own filter matches only `name === 'AbortError'`, so the disconnect reached `onRequestError` and Sentry as an application error | (this commit) | `tests/unit/observability/request-error.test.ts` — the disconnect is dropped, an ordinary render error, a real stream write failure, a message that merely quotes the sentence, and a non-`Error` throw are all still reported (D-721) | fixed |
| QA-045 | P1 | On **Runs**, an assignment the student had a live run on could read **Not started** with **Start** beside it, and pressing Start refused them — a dead end over a run that existed. Reproduced in the suite's own database, where 100 of the 106 rows carried a run and the four oldest assignments printed "Not started" over runs their newer twins showed as Working and Scored | The screen joined two independently paged lists: the assignments of the page it was drawing against "the hundred newest runs of the seat". Past a hundred runs the older attempts fell outside that window and `toRunListRows` emitted their assignments with `run: null` | (this commit) | `tests/integration/runs/start.test.ts` "returns an attempt the newest-hundred window has left behind" — a hundred newer runs on a second assignment, the old attempt absent from the window and present in the assignment-keyed read (D-722) | fixed |
| QA-046 | P2 | Ten of the fifteen failures of the first clean three-engine ×3 run, and the eleventh: `meridian-roast` was "not found" on **Packages** for firefox and webkit, and **Decision Run 1 (walkthrough)** was "not found" on **Runs** for webkit — while the screens were correct and the rows were one page further back | Three specs assumed a seeded row sits on the first page. Every list is a cursor page ordered `created_at desc, id desc` (D-020), so a seeded row is the oldest there is, and five specs add packages and twenty-five add assignments on every engine pass; chromium passed only because it runs first | (this commit) | `walkPagesTo()` in `tests/e2e/fixtures.ts`, used by `smoke/screens.spec.ts` (courses and runs), `author/packages.spec.ts` and `a11y/author.spec.ts` — which proves the list and its paging together and still asserts exactly as strictly against production's fresh database (D-718) | fixed |
| QA-047 | P2 | The QA prompt's fourth Playwright project — Mobile Safari — did not exist, so nothing proved the demo surface on a phone with touch and an iPhone user agent | `playwright.config.ts` had chromium, firefox, webkit and the guide chain only; the 390×844 reading in C11 was done by eye from the production build | (this commit) | `playwright.config.ts` project `mobile-safari` on `devices['iPhone 14']`, running the eight smoke specs — both seats signing in, one screen per persona, the bottom rail and the header menus as taps (D-723) | fixed |
| QA-048 | P2 | Nothing joined the requirement register to COVERAGE.md or to the tests it names, so a renamed spec or a dropped requirement would have left a row that read as coverage and proved nothing. One dangling cross-reference was already there: FR-100's notes sent a reader to FR-158, which the register never issued (it means FR-191) | The two joins — requirement to row, row to test file — were kept by hand | (this commit) | `tests/unit/docs/coverage-paths.test.ts`: 329 ids issued once each, covered exactly once with the ranges opened out, every referenced id issued, and every test path a row names opened (D-716) | fixed |
| QA-049 | P1 | `pnpm qa:all` — the QA report's own "re-run everything" command — ran the engine suites and the guide chain in one `playwright test`. The guide chain's first project depends on nothing, so Playwright was free to schedule its reset beside a walkthrough spec and delete the run that spec was taking; and the chain reached **Packages** on a shelf twenty suite packages deep, where the row its own step names is behind "Show more packages" | The two lanes were added as projects of one config (D-707) without anything making them two invocations, and the guide reset purged only rows named "Guide …" | (this commit) | `package.json`: `test:e2e` names the four engine projects, `qa:all` runs it then `test:guides`; `tests/e2e/global-setup.ts` `resetGuideData` also purges the suite's packages (D-724) | fixed |
