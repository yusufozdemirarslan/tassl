# gitleaks fingerprints that are documented non-secret defaults in commits that predate the
# regex allowlist (D-169). Format: <commit>:<file>:<rule>:<line>.
#
# Prefer a content allowlist in `.gitleaks.toml` to a fingerprint here. A fingerprint names the
# commit that introduced the line, and this repository squash-merges: the branch commit a waiver
# was written and verified against stops existing the moment the PR lands, so the waiver silently
# stops applying to the identical line under its new SHA on `main` (D-682). The five waivers that
# used to sit below were all in that state.
cc4e668de6f2b12d1a27196dd75591284d12239d:src/server/db/seed.ts:generic-api-key:22
# A scenario family key a test types into the seed form, in the commit that introduced it: the
# generic-api-key rule reads any hyphenated slug carrying digits as a credential. The line itself
# now carries a `gitleaks:allow` marker, which covers the working tree but not the history.
9651a27d4b6710aae67057bb2a0a190d835f1f6a:tests/unit/components/packages/seed-form.test.tsx:generic-api-key:186
